---
phase: 02-core-analyze-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/gtmetrix.py
  - client/parsers.py
  - tests/conftest.py
  - tests/test_client.py
  - tests/test_parsers.py
autonomous: true
requirements:
  - TEST-01
  - REPT-01
  - REPT-02
  - REPT-03

must_haves:
  truths:
    - "start_test(url) sends a POST /tests request with correct JSON:API body and returns unwrapped test data with an id"
    - "get_test(test_id) fetches test status and returns unwrapped data (test or report type)"
    - "get_report(report_id) fetches report attributes including Core Web Vitals fields"
    - "get_resource(report_id, name) fetches raw JSON sub-resources (lighthouse, HAR) without JSON:API unwrapping"
    - "extract_vitals() returns LCP, TBT, CLS, performance_score, structure_score from a report dict"
    - "filter_failing_audits() returns only audits with score < 1, excluding notApplicable/manual/informative"
    - "extract_top_resources() returns top 10 resources sorted by duration descending, handling missing _transferSize"
  artifacts:
    - path: "client/gtmetrix.py"
      provides: "start_test, get_test, get_report, get_resource methods"
      contains: "async def start_test"
    - path: "client/parsers.py"
      provides: "extract_vitals, filter_failing_audits, extract_top_resources functions"
      exports: ["extract_vitals", "filter_failing_audits", "extract_top_resources"]
    - path: "tests/test_client.py"
      provides: "Unit tests for new client methods"
      contains: "test_start_test"
    - path: "tests/test_parsers.py"
      provides: "Unit tests for new parser functions"
      contains: "test_extract_vitals"
  key_links:
    - from: "client/gtmetrix.py"
      to: "client/parsers.py"
      via: "unwrap_jsonapi import"
      pattern: "from client\\.parsers import unwrap_jsonapi"
    - from: "client/gtmetrix.py start_test()"
      to: "httpx POST /tests"
      via: "self._client.post"
      pattern: "self\\._client\\.post.*tests"
    - from: "client/parsers.py extract_vitals()"
      to: "report dict attributes"
      via: "dict.get() for each CWV field"
      pattern: "report\\.get.*performance_score"
---

<objective>
Add GTMetrix client methods for the test lifecycle (start, poll, report, sub-resources) and parser functions for extracting Core Web Vitals, failing Lighthouse audits, and top HAR resources.

Purpose: Build the data layer that Plan 02's analyze orchestrator will compose. All functions are pure or simple HTTP wrappers, easy to unit test in isolation.
Output: Extended client/gtmetrix.py, extended client/parsers.py, full test coverage for both.
</objective>

<execution_context>
@/Users/ltkort/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ltkort/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-analyze-workflow/02-RESEARCH.md

<interfaces>
<!-- Existing interfaces from Phase 1 that this plan extends -->

From client/gtmetrix.py:
```python
class GTMetrixClient:
    def __init__(self, api_key: str) -> None: ...
    async def __aenter__(self) -> "GTMetrixClient": ...
    async def __aexit__(self, *args) -> None: ...
    async def get_status(self) -> dict: ...
    # self._client is httpx.AsyncClient with base_url, auth, headers, follow_redirects=True
```

From client/parsers.py:
```python
def unwrap_jsonapi(response: dict) -> dict:
    """Strips JSON:API envelope: returns flat dict with id, type, and all attributes."""
```

From tests/conftest.py:
```python
@pytest.fixture
def mock_client():
    """GTMetrixClient mock returning successful status response."""

@pytest.fixture
def error_client():
    """GTMetrixClient mock raising HTTP 401."""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add client methods for test lifecycle</name>
  <files>client/gtmetrix.py, tests/test_client.py, tests/conftest.py</files>
  <behavior>
    - test_start_test: mock_client.start_test("https://example.com") sends POST /tests with JSON:API body {"data": {"type": "test", "attributes": {"url": "https://example.com"}}}, returns unwrapped dict with "id" and "state" keys
    - test_get_test: mock_client.get_test("KtbMoPEq") sends GET /tests/KtbMoPEq, returns unwrapped dict (could be type "test" or "report")
    - test_get_report: mock_client.get_report("KtbMoPEq") sends GET /reports/KtbMoPEq, returns unwrapped dict with CWV fields
    - test_get_resource: mock_client.get_resource("KtbMoPEq", "lighthouse") sends GET /reports/KtbMoPEq/resources/lighthouse, returns raw JSON (NOT unwrapped through unwrap_jsonapi)
    - test_start_test_http_error: raises httpx.HTTPStatusError on non-2xx
  </behavior>
  <action>
    1. Add mock response fixtures to tests/conftest.py:
       - MOCK_TEST_RESPONSE: JSON:API wrapped test creation response (202) with id "KtbMoPEq", state "queued", meta.credits_left
       - MOCK_TEST_COMPLETED_RESPONSE: JSON:API wrapped test with state "completed" and links.report
       - MOCK_REPORT_RESPONSE: JSON:API wrapped report with performance_score, structure_score, largest_contentful_paint, total_blocking_time, cumulative_layout_shift
       - MOCK_LIGHTHOUSE_RESPONSE: Raw Lighthouse JSON (not JSON:API) with 3 sample audits (one passing score=1, one failing score=0.45, one notApplicable)
       - MOCK_HAR_RESPONSE: Raw HAR JSON with log.entries containing 12 entries (to test top-10 extraction), varying time and _transferSize values, one entry with time=0 (cached), one entry missing _transferSize

    2. Write tests in tests/test_client.py using httpx mock transport pattern. For each client method, create an httpx.AsyncClient with a MockTransport that captures the request and returns the appropriate mock response. Instantiate GTMetrixClient, replace self._client with the mock transport client, and call the method.

    3. Add four methods to GTMetrixClient in client/gtmetrix.py:
       - start_test(self, url: str) -> dict: POST /tests with JSON:API body, raise_for_status(), return unwrap_jsonapi(response.json()). Also extract and include meta.credits_left from the raw response before unwrapping.
       - get_test(self, test_id: str) -> dict: GET /tests/{test_id}, raise_for_status(), return unwrap_jsonapi(response.json())
       - get_report(self, report_id: str) -> dict: GET /reports/{report_id}, raise_for_status(), return unwrap_jsonapi(response.json())
       - get_resource(self, report_id: str, resource_name: str) -> dict: GET /reports/{report_id}/resources/{resource_name}, raise_for_status(), return response.json() directly (NOT unwrap_jsonapi -- sub-resources are raw JSON, not JSON:API)

    Each method must assert self._client is not None (same pattern as get_status).
  </action>
  <verify>
    <automated>cd /Users/ltkort/Sites/Devtools/Tracking/gtmetrix-mcp-serer && uv run pytest tests/test_client.py -x -q</automated>
  </verify>
  <done>All four client methods exist and pass tests. start_test sends correct JSON:API POST body. get_resource returns raw JSON without unwrapping. Existing get_status tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add parser functions for report data extraction</name>
  <files>client/parsers.py, tests/test_parsers.py</files>
  <behavior>
    - test_extract_vitals: given a report dict with performance_score=85, structure_score=72, largest_contentful_paint=1200, total_blocking_time=150, cumulative_layout_shift=0.05 -> returns dict with those 5 fields (keys: performance_score, structure_score, largest_contentful_paint_ms, total_blocking_time_ms, cumulative_layout_shift)
    - test_extract_vitals_missing_fields: given a report dict missing some fields -> returns dict with None for missing fields (never raises)
    - test_filter_failing_audits: given lighthouse JSON with 3 audits (score=1, score=0.45, scoreDisplayMode="notApplicable") -> returns list of 1 audit (the score=0.45 one) with keys id, title, description, displayValue, score
    - test_filter_failing_audits_sorted: given multiple failing audits -> returns sorted by score ascending (worst first)
    - test_filter_failing_audits_excludes_informative: audits with scoreDisplayMode="informative" are excluded even if score < 1
    - test_extract_top_resources: given HAR with 12 entries -> returns top 10 sorted by duration descending, each with url, size_bytes, duration_ms
    - test_extract_top_resources_skips_zero_time: entries with time <= 0 are excluded
    - test_extract_top_resources_fallback_size: when _transferSize is missing, falls back to response.bodySize; when both missing, size_bytes is None
    - test_truncate_url: URLs longer than 120 chars are truncated with "..."
  </behavior>
  <action>
    1. Write tests in tests/test_parsers.py using the MOCK_LIGHTHOUSE_RESPONSE and MOCK_HAR_RESPONSE fixtures from conftest.py (added in Task 1), plus inline test data for edge cases.

    2. Add three public functions and one private helper to client/parsers.py (below the existing unwrap_jsonapi):

       - extract_vitals(report: dict) -> dict: Extract 5 CWV fields using .get() with None defaults. Use "_ms" suffix on time-based keys for clarity.

       - filter_failing_audits(lighthouse_json: dict) -> list[dict]: Iterate lighthouse_json["audits"].items(). Skip if score is None. Skip if scoreDisplayMode in ("notApplicable", "manual", "informative"). Include if score < 1. Extract only: id, title, description, displayValue, score. Sort by score ascending.

       - extract_top_resources(har_json: dict, limit: int = 10) -> list[dict]: Get log.entries. Skip entries with time <= 0. For size: try response._transferSize, fall back to response.bodySize, else None. Truncate URLs via _truncate_url(). Sort by duration_ms descending. Return top N.

       - _truncate_url(url: str, max_length: int = 120) -> str: If len(url) <= max_length return as-is, else url[:max_length-3] + "..."
  </action>
  <verify>
    <automated>cd /Users/ltkort/Sites/Devtools/Tracking/gtmetrix-mcp-serer && uv run pytest tests/test_parsers.py -x -q</automated>
  </verify>
  <done>Three parser functions and one helper exist. All tests pass including edge cases (missing fields, zero-time entries, URL truncation). Existing unwrap_jsonapi tests still pass.</done>
</task>

</tasks>

<verification>
cd /Users/ltkort/Sites/Devtools/Tracking/gtmetrix-mcp-serer && uv run pytest tests/ -x -q
All tests pass including Phase 1 tests (no regressions).
</verification>

<success_criteria>
- GTMetrixClient has start_test, get_test, get_report, get_resource methods
- parsers.py has extract_vitals, filter_failing_audits, extract_top_resources functions
- All new functions have unit tests covering happy path and edge cases
- No regressions in existing Phase 1 tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-analyze-workflow/02-01-SUMMARY.md`
</output>
