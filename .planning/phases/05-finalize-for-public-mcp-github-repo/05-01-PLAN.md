---
phase: 05-finalize-for-public-mcp-github-repo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - LICENSE
  - .gitignore
autonomous: true
requirements:
  - PUB-01
  - PUB-02
  - PUB-03

must_haves:
  truths:
    - "pyproject.toml has correct project name 'gtmetrix-mcp-server' and version '1.0.0'"
    - "MIT LICENSE file exists in repo root"
    - ".planning/ directory is untracked by git"
  artifacts:
    - path: "pyproject.toml"
      provides: "Correct project name, version, metadata, classifiers, URLs"
      contains: "gtmetrix-mcp-server"
    - path: "LICENSE"
      provides: "MIT license text"
      contains: "MIT License"
    - path: ".gitignore"
      provides: "Excludes .planning/ from tracking"
      contains: ".planning/"
  key_links:
    - from: "pyproject.toml"
      to: "CHANGELOG.md"
      via: "version number"
      pattern: 'version = "1.0.0"'
---

<objective>
Fix pyproject.toml metadata (name typo, version, classifiers, URLs, license), create MIT LICENSE file, and remove .planning/ from git tracking.

Purpose: Correct packaging metadata and repo hygiene for public release.
Output: Production-ready pyproject.toml, LICENSE file, clean .gitignore.
</objective>

<execution_context>
@/Users/ltkort/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ltkort/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-finalize-for-public-mcp-github-repo/05-CONTEXT.md
@.planning/phases/05-finalize-for-public-mcp-github-repo/05-RESEARCH.md

<interfaces>
<!-- Current pyproject.toml needs these changes:
- name: "gtmetrix-mcp-serer" -> "gtmetrix-mcp-server"
- version: "0.1.0" -> "1.0.0"
- description: "Add your description here" -> concise MCP server description
- Add: license, authors, classifiers, [project.urls]
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix pyproject.toml metadata and create LICENSE</name>
  <files>pyproject.toml, LICENSE</files>
  <action>
Update pyproject.toml [project] section:
- Fix name: "gtmetrix-mcp-serer" -> "gtmetrix-mcp-server"
- Set version: "1.0.0" (matches CHANGELOG.md)
- Set description: "MCP server for GTMetrix website performance analysis" (or similar concise wording, Claude's discretion)
- Add: license = "MIT" (PEP 639 SPDX string, NOT the table format)
- Add: requires-python = ">=3.11" (already present, keep it)
- Add: readme = "README.md" (already present, keep it)
- Add authors: [{ name = "Marshmallow", email = "info@marshmallow.dev" }] (email at Claude's discretion)
- Add classifiers:
  - "Development Status :: 5 - Production/Stable"
  - "License :: OSI Approved :: MIT License"
  - "Programming Language :: Python :: 3.11"
  - "Programming Language :: Python :: 3.12"
  - "Programming Language :: Python :: 3.13"
  - "Topic :: Software Development :: Testing"
  - "Topic :: Internet :: WWW/HTTP"
- Add [project.urls] section:
  - Homepage = "https://github.com/marshmallow-packages/gtmetrix-mcp"
  - Repository = "https://github.com/marshmallow-packages/gtmetrix-mcp.git"
  - Issues = "https://github.com/marshmallow-packages/gtmetrix-mcp/issues"
- Keep existing [dependency-groups], [tool.pytest.ini_options], [tool.ruff] sections unchanged.

Create LICENSE file in repo root with standard MIT license text:
- Copyright (c) 2026 Marshmallow
- Full MIT license text (see 05-RESEARCH.md for exact text)
  </action>
  <verify>
    <automated>grep -q 'gtmetrix-mcp-server' pyproject.toml && grep -q 'version = "1.0.0"' pyproject.toml && grep -q 'MIT License' LICENSE && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>pyproject.toml has correct name "gtmetrix-mcp-server", version "1.0.0", full metadata with classifiers and URLs. LICENSE file exists with MIT text.</done>
</task>

<task type="auto">
  <name>Task 2: Add .planning/ to .gitignore and remove from tracking</name>
  <files>.gitignore</files>
  <action>
1. Add `.planning/` to .gitignore FIRST (add at end with a "# Planning docs (internal)" comment).
2. Run: git rm -r --cached .planning/
   This removes .planning/ from git tracking without deleting the local files.
   Note: This will show as many deleted files in git status -- that is expected and correct.

Important: Add to .gitignore BEFORE running git rm --cached (per pitfall #3 in research).
  </action>
  <verify>
    <automated>grep -q '.planning/' .gitignore && ! git ls-files --error-unmatch .planning/STATE.md 2>/dev/null && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>.planning/ is in .gitignore. .planning/ files are no longer tracked by git (but still exist locally).</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -x` passes (no regressions from metadata changes)
- `uv run ruff check .` passes
- `git ls-files .planning/` returns nothing
- `grep 'gtmetrix-mcp-server' pyproject.toml` matches
- LICENSE file exists
</verification>

<success_criteria>
- pyproject.toml has correct name, version 1.0.0, MIT license, classifiers, author, URLs
- LICENSE file contains MIT license text with Marshmallow copyright
- .planning/ is in .gitignore and untracked by git
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-finalize-for-public-mcp-github-repo/05-01-SUMMARY.md`
</output>
