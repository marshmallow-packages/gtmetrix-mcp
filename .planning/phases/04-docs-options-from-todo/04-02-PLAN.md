---
phase: 04-docs-options-from-todo
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - README.md
  - CHANGELOG.md
  - .env.example
autonomous: true
requirements:
  - TODO-README
  - TODO-CHANGELOG
  - TODO-ENVEXAMPLE
must_haves:
  truths:
    - "README documents all three tools with updated signatures including new params"
    - "README has a Reference section with location, browser, and device tables"
    - "PRO-only features are marked with (PRO) tag in reference tables"
    - "CHANGELOG follows Keep a Changelog format with backfilled v1.0 entry"
    - ".env.example has commented-out lines for all default env vars"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Project changelog with v1.0 backfill"
      contains: "[1.0.0]"
    - path: "README.md"
      provides: "Updated docs with new params and reference tables"
      contains: "Reference"
    - path: ".env.example"
      provides: "Example env with default param vars"
      contains: "GTMETRIX_DEFAULT_BROWSER"
  key_links:
    - from: "README.md"
      to: "tools/analyze.py"
      via: "Tool signatures and parameter docs match implementation"
      pattern: "gtmetrix_analyze.*browser.*device.*adblock"
---

<objective>
Create CHANGELOG.md, update README with new parameter docs and reference tables, and update .env.example with new default env vars.

Purpose: Complete the documentation todos -- README with install instructions (already exists, needs param updates), changelog backfilling v1.0, and env example for new configurable defaults.
Output: CHANGELOG.md, updated README.md, updated .env.example.
</objective>

<execution_context>
@/Users/ltkort/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ltkort/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-docs-options-from-todo/04-CONTEXT.md
@.planning/phases/04-docs-options-from-todo/04-01-SUMMARY.md

<interfaces>
<!-- From plan 01, the new tool signature will be: -->
```python
async def gtmetrix_analyze(
    url: str,
    location: str | None = None,
    browser: str | None = None,
    device: str | None = None,
    adblock: str | None = None,
    *, ctx: Context,
) -> dict:
```

<!-- Config fields added in plan 01: -->
```python
gtmetrix_default_browser: str | None = None
gtmetrix_default_device: str | None = None
gtmetrix_default_adblock: str | None = None
```

<!-- Device aliases from plan 01: -->
```python
DEVICE_ALIASES = {
    "phone": "iphone_16",
    "tablet": "ipad_air",
    "desktop": None,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CHANGELOG.md and update .env.example</name>
  <files>CHANGELOG.md, .env.example</files>
  <action>
1. Create CHANGELOG.md in Keep a Changelog format (keepachangelog.com). Backfill all completed work as a single v1.0.0 entry dated 2026-03-05. Include tool-level detail grouped by Added:

```markdown
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/),
and this project adheres to [Semantic Versioning](https://semver.org/).

## [1.0.0] - 2026-03-05

### Added
- `gtmetrix_analyze(url)` tool -- full performance analysis returning Core Web Vitals, failing Lighthouse audits, and top 10 slowest resources
- `gtmetrix_check_status()` tool -- API credits remaining, account type, and refill date
- `gtmetrix_list_locations()` tool -- available test locations with in-memory caching
- Location parameter for `gtmetrix_analyze` with validation against available locations
- Browser, device, and adblock parameters for `gtmetrix_analyze`
- Configurable parameter defaults via `GTMETRIX_DEFAULT_*` environment variables
- Device aliases: `phone`, `tablet`, `desktop` (or pass raw GTMetrix device IDs)
- Automatic test polling (3-second intervals, 5-minute timeout)
- Structured error responses with actionable hints for all failure modes
- JSON:API response parsing for all GTMetrix API v2.0 endpoints
- stdio transport for Claude Code MCP integration
```

2. Update .env.example to add commented-out lines for the new default env vars:

```
# GTMetrix API key -- get yours at https://gtmetrix.com/api/
# Copy this file to .env and fill in your actual key
GTMETRIX_API_KEY=your_api_key_here

# Optional: Default test parameters
# If set, these are used when the corresponding tool parameter is not provided.
# Explicit tool parameters always override these defaults.
# GTMETRIX_DEFAULT_LOCATION=1
# GTMETRIX_DEFAULT_BROWSER=3
# GTMETRIX_DEFAULT_DEVICE=phone
# GTMETRIX_DEFAULT_ADBLOCK=0
```
  </action>
  <verify>
    <automated>test -f CHANGELOG.md && grep -q "1.0.0" CHANGELOG.md && grep -q "GTMETRIX_DEFAULT_BROWSER" .env.example && echo "OK"</automated>
  </verify>
  <done>CHANGELOG.md exists with v1.0.0 backfill in Keep a Changelog format, .env.example has commented-out default param vars</done>
</task>

<task type="auto">
  <name>Task 2: Update README with new params and reference tables</name>
  <files>README.md</files>
  <action>
Update the existing README.md (do NOT rewrite from scratch -- extend it):

1. **Tools table**: Update the tool signatures to show new params:
   ```
   | `gtmetrix_analyze(url, location?, browser?, device?, adblock?)` | Run a full performance test... |
   ```

2. **Add a "Configuration" section** after "Running Tests" and before "Project Structure", documenting env var defaults:

   ```markdown
   ## Configuration

   All parameters can have defaults set via environment variables. Explicit tool parameters always override defaults.

   | Environment Variable | Description | Example |
   |---------------------|-------------|---------|
   | `GTMETRIX_API_KEY` | **Required.** Your GTMetrix API key | `abc123` |
   | `GTMETRIX_DEFAULT_LOCATION` | Default test location ID | `1` |
   | `GTMETRIX_DEFAULT_BROWSER` | Default browser ID | `3` |
   | `GTMETRIX_DEFAULT_DEVICE` | Default device (`phone`, `tablet`, `desktop`, or raw ID) | `phone` |
   | `GTMETRIX_DEFAULT_ADBLOCK` | Enable adblock by default (`0` or `1`) | `0` |
   ```

3. **Add a "Reference" section** after "Project Structure" and before "Example Response", with three tables:

   **Device Aliases** table:
   | Alias | GTMetrix Device ID | Description |
   |-------|-------------------|-------------|
   | `phone` | `iphone_16` | Modern smartphone viewport |
   | `tablet` | `ipad_air` | Tablet viewport |
   | `desktop` | _(default)_ | No simulated device (PRO) |

   Note: Simulated devices require a GTMetrix PRO account. You can also pass any raw GTMetrix device ID directly.

   **Common Browser IDs** table (from GTMetrix API docs):
   | ID | Browser |
   |----|---------|
   | `3` | Chrome (Desktop) |

   Note: Use `gtmetrix_list_locations()` to see which browsers are available per location. Browser IDs may vary by account.

   **Common Location IDs** table (mention to use gtmetrix_list_locations for full list):
   Note: Locations vary by account. Use `gtmetrix_list_locations()` to see your available locations with IDs.

4. **Update example prompts** to include new param examples:
   - "Analyze https://example.com with adblock enabled"
   - "Test https://example.com on a phone from London"

5. **Update test count** in Project Structure if it changed.
  </action>
  <verify>
    <automated>grep -q "browser" README.md && grep -q "device" README.md && grep -q "adblock" README.md && grep -q "Reference" README.md && grep -q "Configuration" README.md && echo "OK"</automated>
  </verify>
  <done>README has updated tool table with new params, Configuration section with env var table, Reference section with device/browser/location tables, PRO features marked with (PRO), example prompts updated</done>
</task>

</tasks>

<verification>
- CHANGELOG.md exists and follows Keep a Changelog format
- README.md has Configuration and Reference sections
- .env.example has all GTMETRIX_DEFAULT_* vars (commented out)
- All tool signatures in README match actual implementation
</verification>

<success_criteria>
- CHANGELOG.md has complete v1.0.0 backfill with all tools and features
- README documents all params for gtmetrix_analyze including new browser/device/adblock
- Reference tables show device aliases with (PRO) tags where applicable
- .env.example shows all configurable defaults as commented-out lines
- README example prompts demonstrate new parameter usage
</success_criteria>

<output>
After completion, create `.planning/phases/04-docs-options-from-todo/04-02-SUMMARY.md`
</output>
